# Secrets — never commit (template: config/secrets.example.json)
config/secrets.json

# Runtime state (regenerated by the ticks/capabilities)
state/
*.lock
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Legacy parts bin (lives outside this repo; see KNOWN-ISSUES.md)
_oldrepo

# Python
__pycache__/
*.pyc
.pytest_cache/

# OS
.DS_Store

# Coder isolation worktrees
worktrees/
